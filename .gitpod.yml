tasks:
  - init: |
      date
      docker-compose build web
      date

    command: |
      date
      docker-compose up -d web postgres redis
      date

github:
  prebuilds:
    master: true
    branches: true
